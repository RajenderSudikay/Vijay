@model MedProSC.Project.Website.Models.HeaderModel

@if (Model != null)
{
    <div class="container-fluid">
        <div id="navbar">
            <a class="header-label">@Model.HeaderTitle</a>
            <a class="header-builder">@Model.FormBuilderTitle</a>
            <a class="header-history">@Model.BundleHistoryTitle</a>
        </div>
    </div>

}